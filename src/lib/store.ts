import { localStorage, sessionStorage, storage } from './storage';

export const gradNurseID = localStorage('GRAD_NURSE_ID', undefined);
export const virtualNurseID = localStorage('MENTORED_NURSE_ID', undefined);